{
  "_comment": "this project is not a node.js one, package.json is just used to define some metadata",
  "scripts": {
    "commit": "commit",
    "precommit": "pre-commit run -a",
    "preinstall": "npx force-resolutions",
    "secrets": "gitleaks detect --config .gitlab/config/.gitleaks.toml --verbose"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  },
  "devDependencies": {
    "@commitlint/cli": "17.6.1",
    "@commitlint/config-conventional": "17.6.1",
    "@commitlint/prompt-cli": "17.0.0",
    "@semantic-release/changelog": "6.0.3",
    "@semantic-release/commit-analyzer": "9.0.2",
    "@semantic-release/exec": "6.0.3",
    "@semantic-release/git": "10.0.1",
    "@semantic-release/gitlab": "12.0.1",
    "@semantic-release/release-notes-generator": "10.0.3",
    "semantic-release": "21.0.1"
  }
}
